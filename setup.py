from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='From data to neural network to chip',
    author='Joey van Rijn',
    license='',
)
